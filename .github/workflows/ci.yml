name: Hello World Workflow

on:
  push:
    branches: [ main ]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "👋 Hello World von deiner ersten GitHub Action!"
